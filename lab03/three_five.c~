#include <stdio.h> 

int main (void) {
    int number = 1; 
    printf("Enter number: ");
    scanf("%d", &number);
    int i = 1;
    while (i < number){ 
        if (i%3 == 0) { 
            printf("%d \n", i);
        } else if (i % 5 == 0) { 
            printf("%d \n", i);
        } 
        i = i +1; 
    }
    return 0;
}
